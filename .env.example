#Postgres
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=

# BigQuery
BQ_PROJECT=
BQ_DATASET=fx_dataset
# Service-account auth only — leave unset if using `gcloud auth application-default login`
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json